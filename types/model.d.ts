export interface IColor {
    id: string;
    name: string;
    hex?: string;
}

export interface IBrand {
    id: string;
    name: string;
    slug: string;
}

export interface ICategory {
    id: string;
    name: string;
    slug: string;
}